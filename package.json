{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "build:watch": "learn run build:watch"
  },
  "devDependencies": {
    "lerna": "^3.8.0"
  }
}
